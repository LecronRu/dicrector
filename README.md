Название произошло от Dictionary corrector. Компонент применяющий к текстовой строке правила замены,
объединенные в различные словари, определяющие алгоритм замены. Имеет много общего с коррекцией
в программах Балаболка и Демагог, которые и послужили примером. Но более гибкий, позволяя дополнять
новыми алгоритмами в виде плагинов формата. В том числе, подключать в поток обработки сторонние программы
типа `ruaccent` или `silero-stress`; хранить правила в базах данных, подгружая их статически, в виде обычного
словаря или динамически, извлекать запросом конкретное слово.

### Установка компонента.
`pip install . ` находясь в папке пакета или `pip install path/to/package` из любого другого 
места.

### Использование компонента.
 ```python
from dicrector import Corrector
corrector = Corrector(['словарь1', ...])
result = corrector.execute('line string')
```
Путь к словарю строка или pathlib.Path

### Предустановленные форматы.
* `dic`: простые правила поиска и замены. Аналог используемых в Балаболка, с одним ограничением — только одиночные 
  слова.
* `dicx`: расширенные правила поиска и замены, dic(eXtended). Допускаются словосочетания, в том числе с
подстановочным символом `*` внутри шаблона поиска.
* `rex`: правила на основе регулярных выражений;
* `rexw`: аналогично rex, но с областью проверки слова, rex(Word).
* `exts`: шлюз для подключения внешних обработчиков. Обрабатывается каждое предложение строки.
* `extw`: аналогично `exts`, но для слов.

Детальные описания формата правил и модификаторов [здесь](doc/formats.md).

### Некоторые принципы работы.
Для словарей указывается область применения — строка, предложение, слово. В соответствии с этим,
программа разбивает строку на предложения, а предложения на слова. При обратном слиянии, все изменения фиксируются.
Процедура не быстрая.  Если после замены слова, понадобился текст предложения, а потом опять слово,
будет произведено слияние и повторная токенизация. Поэтому словари стоит располагать в порядке убывания
и последующего увеличения области видимости — строка, предложение, слово, предложение, строка.

Применяемые словари не группируются и правила в них не сортируются, это ответственность пользователя.
Внутренний символ ударения — \` (обратный апостроф) после ударной гласной (ударе\`ние). Другие символы библиотекой 
`razdel` будут восприняты как разделители слов (настраивается в файле `textparse.py`). 


### Пользовательские форматы.
Можете создать свой формат обработки, написав модуль аналогичный расположенным в папке `formats` и зарегистрировав 
его до загрузки соответствующих словарей. Используемый формат определяется по разрешению файла словаря.
```Python
from dicrector import Formats, Corrector
from pathlib import Path

newformat = Path(r'path/to/module')
Formats.register(newformat)

or

user_formats = Path(r'path/to/modules_dir')
Formats.register_all(user_formats)

corrector = Corrector(['demo.newformat'])
```

Подробнее [здесь](doc/plugin.md).

 
### Лицензия.
[Creative Commons BY (CC BY)](https://creativecommons.org/licenses/by/4.0/).
Разрешается использование, включая коммерческое, с обязательным указание авторства и обозначения изменений,
если таковые были сделаны.